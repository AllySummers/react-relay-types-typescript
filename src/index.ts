import ProfilerContextActual from 'react-relay/lib/relay-hooks/ProfilerContext';
import ProfilerContextDefs from 'react-relay/relay-hooks/ProfilerContext';

// so typescript and babel don't strip unused imports
console.log({
    ProfilerContextActual,
    ProfilerContextDefs
});